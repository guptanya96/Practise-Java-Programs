@Test
    //2 sum problem - problem where you are given an array and a target value and you have to find the indices of the elements in the array that add up to the target value.
    public void twoSumProbUsing2Loops() {
        int array[] = {5, 2, 1, 4, 7};
        int check = 6;
        for (int i = 0; i < array.length; i++) {
            for (int j = i + 1; j < array.length; j++) {
                if (array[i] + array[j] == check) {
                    System.out.println("1st element: " + array[i] + " at position: " + i + " Second element: " + array[j] + " at position " + j);
                }
            }

        }
    }

Output:1st element: 5 at position: 0 Second element: 1 at position 2
1st element: 2 at position: 1 Second element: 4 at position 3
