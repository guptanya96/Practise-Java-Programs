@Test
    //move negative to start
    public void moveNegative() {
        int[] arr = {-1, 2, 3, -4, -7, 8};
        List<Integer> li = new ArrayList<>();
        List<Integer> li1 = new ArrayList<>();
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] < 0) {
                li.add(arr[i]);
            } else {
                li1.add(arr[i]);
            }
        }
        li.addAll(li1);
        System.out.println(li);

    }

Output:[-1, -4, -7, 2, 3, 8]
