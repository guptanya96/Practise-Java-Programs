 @Test
    //string compression or replace duplicate char with their occurence number
   
    public void stringCompress() {
        String value = "aaabbccddddeaa";

        StringBuilder sb = new StringBuilder();
        int count = 1;

        for (int i = 1; i <= value.length(); i++) {
            if (i < value.length() && value.charAt(i) == value.charAt(i - 1)) {
                count++;
            } else {
                sb.append(value.charAt(i - 1)).append(count);
                count = 1;
            }
        }

        System.out.print(sb.toString());
    }
}


Output: a3b2c2d4e1
