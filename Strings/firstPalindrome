@Test //find all palindrome strings
        public void firstPalindrome() {
        String words[] = {"abc","car","ada","racecar","cool"};
            for(int i=0;i<words.length;i++)
            {
                StringBuffer sb = new StringBuffer(words[i]);
                sb.reverse();
                if(sb.toString().equals(words[i]))
                {

                    System.out.println(words[i]);
                }
                else
                {
                    System.out.println("No palindrome strings present");
                }

            }

        }
Output: No palindrome strings present
No palindrome strings present
ada
racecar
No palindrome strings present
